server:
  port: 9296

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/TeamLeadDemo/microservices-cloud-config
          clone-on-start: true